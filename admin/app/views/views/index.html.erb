<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", views_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_view_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Views</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.view.display_type", :default => t("activerecord.labels.display_type", :default => "Display_type")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @views.each do |view| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= view.id %>
          </td>
                    <td>
            <%= link_to view.display_type, view_path(view) %>
          </td>
                    <td>
            <%= view.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", view_path(view) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_view_path(view) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", view_path(view), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
